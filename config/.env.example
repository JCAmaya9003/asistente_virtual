# Copiá este archivo a .env y completá tus claves. .env está en .gitignore.
# Ninguna clave se commitea NUNCA.

# API del clima (se usa cuando se implemente la skill de clima)
WEATHER_API_KEY=

# Endpoint de Ollama (v5). Local por defecto.
OLLAMA_HOST=http://127.0.0.1:11434
