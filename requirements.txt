# v0 — núcleo y REPL
PyYAML>=6.0
pytest>=8.0

# v1 — voz de salida (TTS local con Piper, GPL-3.0)
piper-tts>=1.4

# v2 — oído (STT local con faster-whisper + captura de micrófono)
faster-whisper>=1.1
sounddevice>=0.4
numpy>=1.24

# v2 GPU (Windows) — cuBLAS y cuDNN 9 dentro del venv.
# Sin esto, faster-whisper con CUDA falla: 'cublas64_12.dll is not found'.
# adapters/stt_whisper.py registra estas DLLs automáticamente en Windows.
nvidia-cublas-cu12
nvidia-cudnn-cu12>=9,<10

# v3 — integración con el sistema (hotkey global + ícono de bandeja)
# pynput y NO 'keyboard': esta última a veces exige privilegios de administrador.
pynput>=1.7
pystray>=0.19
Pillow>=10.0

# v4 — wake word. El detector corre en CPU con consumo despreciable.
# torch/onnx SOLO se usan para ENTRENAR (scripts/entrenar_wakeword.py), no en runtime.
# No hace falta Docker: openWakeWord trae los extractores de features preentrenados.
openwakeword>=0.6
torch>=2.0
onnx>=1.15
onnxscript>=0.1

# --- se agregan cuando cada versión los pida (NO instalar aún) ---
# v0.5 (borrado seguro):  Send2Trash
# v5   (router LLM):      ollama
